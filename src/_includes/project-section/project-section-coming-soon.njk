<section id="project-{{ project.name | slug }}" class="bg-{{ project.colors.background }} py-24">
  <div class="mx-auto max-w-7xl px-6">
    <div class="max-w-4xl">
      <p class="text-sm uppercase tracking-[.35em] text-{{ project.colors.accent }} font-subheading">Coming soon</p>
      <h2 class="mt-4 text-5xl font-heading font-semibold tracking-tight text-{{ project.colors.mainHeading }} sm:text-6xl">{{ project.name }}</h2>
      <p class="mt-6 text-xl leading-9 text-{{ project.colors.body }}">{{ project.description }}</p>
    </div>
    <div class="mt-12 grid gap-6 sm:grid-cols-3">
      {% for feature in project.projectFeatures %}
      <div class="rounded-3xl border border-white/10 bg-white/10 p-6 shadow-lg backdrop-blur-sm">
        <p class="text-lg font-semibold text-{{ project.colors.accent }}">{{ feature.short }}</p>
        <p class="mt-3 text-base leading-7 text-{{ project.colors.body }}">{{ feature.description }}</p>
      </div>
      {% endfor %}
    </div>
    <div class="mt-12 inline-flex items-center gap-3 rounded-full bg-{{ project.colors.accent }} px-5 py-3 text-{{ project.colors.contrast }} font-semibold shadow-sm">
      <span>Coming soon</span>
      <span aria-hidden="true">→</span>
    </div>
  </div>
</section>
