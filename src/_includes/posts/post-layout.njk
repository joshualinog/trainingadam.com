<article class="post">
  <h1 class="font-heading">{{ post.data.title }}</h1>
  <div class="meta">By {{ post.data.author }} — {{ post.data.createdAt }}</div>
  <div class="content">{{ post.data.content | safe }}</div>
</article>
